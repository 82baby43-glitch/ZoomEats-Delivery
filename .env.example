NEXT_PUBLIC_SUPABASE_URL=https://njrrhckegbfqhwkqkzvw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-from-supabase-dashboard

# Production site URL (auth redirects, SEO):
# NEXT_PUBLIC_SITE_URL=https://zoomeats.net

# Production launch — required on Vercel for frontend Supabase client:
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Optional on Supabase edge: SUPABASE_ANON_KEY (same value as anon key for health audits)

# Admin security — required for production admin dashboard access:
# ADMIN_EMAILS=you@company.com,ops@company.com
# NEXT_PUBLIC_ADMIN_EMAILS=you@company.com,ops@company.com

# Server-only (Vercel + Supabase secrets — never commit real values):
# STRIPE_API_KEY=sk_test_...              # or STRIPE_SECRET_KEY (same value)
# STRIPE_WEBHOOK_SECRET=whsec_...         # Stripe Dashboard → Webhooks signing secret
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...   # optional; server redirect checkout does not require it
# STRIPE_MCP_KEY=ek_test_...              # Cursor MCP only — NOT used by ZoomEats app
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Webhook URL: {SUPABASE_URL}/functions/v1/stripe-webhook (deploy with --no-verify-jwt)
# RECONCILE_CRON_SECRET=random-secret    # For /api/admin/reconcile cron

# Uber Direct (Supabase secrets — never commit real values):
# UBER_DIRECT_CUSTOMER_ID=your-customer-uuid     # Direct Dashboard → Developers
# UBER_DIRECT_CLIENT_ID=your-client-id           # Direct Dashboard → Developers (separate from Customer ID)
# UBER_DIRECT_CLIENT_SECRET=your-client-secret
# UBER_DIRECT_ENABLED=true                       # default true when secrets are set
# UBER_DIRECT_PREFERRED=false                    # skip internal drivers when true
# UBER_DIRECT_DEFAULT_PHONE=+15555550100         # pickup/dropoff fallback phone (E.164)

# Required to apply migrations / configure auth (use ONE for migrate, all three for Google setup):
# SUPABASE_ACCESS_TOKEN=sbp_xxx   # https://supabase.com/dashboard/account/tokens
# DATABASE_URL=postgresql://postgres.njrrhckegbfqhwkqkzvw:PASSWORD@aws-0-us-west-2.pooler.supabase.com:6543/postgres
# GOOGLE_PLACES_API_KEY=AIza...            # Google Places / Maps (server-only — bulk restaurant import)
# Or: GOOGLE_MAPS_API_KEY / GOOGLE_API_KEY

# Menu AI image enhancement (Photoroom — server-only):
# PHOTOROOM_API_KEY=your-photoroom-api-key
# GEOCODE_PROVIDER=auto                    # auto | google | nominatim (nominatim = no Google billing)
# NOMINATIM_API_URL=https://nominatim.openstreetmap.org
# OVERPASS_API_URL=https://overpass-api.de/api/interpreter
# GOOGLE_CLIENT_SECRET=GOCSPX-xxx

# Optional: comma-separated admin emails auto-promoted on login
NEXT_PUBLIC_ADMIN_EMAILS=
