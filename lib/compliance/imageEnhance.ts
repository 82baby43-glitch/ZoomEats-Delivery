/**
 * Deprecated: restaurant onboarding now uses the server-side menu-images
 * enhancement pipeline (`/vendor/menu-images/enhance`, PhotoRoom + public
 * menu-images bucket). This client-side canvas filter is no longer used.
 */

export {};
